[project]
name = "parser_currency_exchange_rate"
version = "0.1.0"
description = "Script for parsing current currency rates relative to USD"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "dotenv>=0.9.9",
    "openpyxl>=3.1.5",
    "pandas>=3.0.3",
    "requests>=2.34.2",
]

[dependency-groups]
dev = [
    "ruff>=0.15.14",
]
